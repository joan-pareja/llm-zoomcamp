# llm-zoomcamp

## Running Kestra

```bash
docker compose --env-file ./.env -f ./03-orchestration/docker-compose.yml up -d
```

Open Kestra at:

```text
http://localhost:8080/
```

If you're running in GitHub Codespaces, use the forwarded port URL for port
`8080`.
